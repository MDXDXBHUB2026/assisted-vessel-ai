// Below this width, AppShell renders SmallScreenNotice instead of the console — see
// docs/assumptions.md for why this is a deliberate gate rather than a responsive rebuild.
export const CONSOLE_MIN_WIDTH_PX = 900
