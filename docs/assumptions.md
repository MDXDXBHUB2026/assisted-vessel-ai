# Assumptions

This document records the assumptions underpinning the Assisted Vessel Intelligence demonstrator. It exists so that anyone reading the other documents can judge how far the illustrative logic can be trusted to generalise, and where it is deliberately simplified for a browser-based prototype.

## Nature of the prototype

1. This is an **independent, standalone concept demonstrator**. It is not derived from, and does not represent, any real shipping company, classification society, shipbuilder, technology vendor, or vessel operator.
2. All vessel identities, fleet names, positions, sensor values, alarms, maintenance records and cargo data are **entirely synthetic**, generated by a client-side simulation engine.
3. The application runs **entirely in the browser**. There is no backend, database, or external network dependency required for the public demonstration.
4. No API keys, credentials, or secrets are used or required.

## Simulation fidelity

5. Physical and machinery models (engine thermal behaviour, fuel consumption, CPA/TCPA geometry, reefer thermal drift) are **illustrative approximations** tuned for a legible, responsive demonstration — not calibrated engineering or hydrodynamic models.
6. Scenario "severity" ramps linearly-ish (via a mean-reverting random walk toward a moving target) over a configurable number of simulated minutes, chosen to make each scenario's effects visible within a reasonable demo session at 1x–8x playback speed.
7. Time compression is deliberate: 1 real second of playback at 1x advances the simulated clock by roughly 30 simulated seconds, so a multi-hour operational condition can be observed within minutes.

## Decision logic

8. The "decision engine" and "safety engine" are **deterministic rule-based systems** written to illustrate what a real analytics/ML and safety-assurance layer might do — they are not trained models and make no claim to predictive accuracy.
9. Confidence, health and anomaly scores are computed from simple weighted formulae against fixed baselines, chosen for plausibility and legibility rather than statistical validity.
10. The Operations Copilot is a **fully deterministic, state-aware responder** with a fixed set of recognised question patterns. It does not call any external AI/LLM service in this prototype.

## Human factors and roles

11. Crew and shore roles (Officer of the Watch, Master, Chief Engineer, Shore Marine Operations, Shore Technical Support, Shore Safety Support) are modelled only to the extent needed to demonstrate authority-gating and role-based review — not as a full crewing or competency model.
12. The demonstrator assumes a single onboard "session" (no multi-user login or concurrent-session conflict handling).

## Data persistence

13. All state (simulation, recommendations, audit trail, shore cases) lives only in the browser tab's memory for the current session. Refreshing the page resets the demonstrator to its baseline condition.
14. "Persistent" in the audit trail requirement is interpreted as *persistent for the duration of the browser session*, not persisted to durable storage — see [SIM-004](requirements-traceability.md).

## Scope boundaries

15. No real vessel, sensor, or shore system is connected. All "conceptual interfaces" described in the architecture are illustrative integration points for a possible future real implementation.
16. Cybersecurity requirements describe the conceptual posture a real system would need; the demonstrator itself has no attack surface beyond a static web page.
17. Connected POC mode is a real, working boundary (it attempts genuine HTTP calls) but there is no backend deployed for the public demonstration — every connected adapter will always report a fallback status, by design, and this is shown honestly rather than simulated as a success.

## Deliberate technology substitutions

A small number of explicitly named technologies were substituted for lighter alternatives, each chosen to keep the demonstrator dependency-lean, fully static, and honest about what it is showing:

18. **No MapLibre GL / deck.gl.** The vessel's geography is entirely synthetic (fictional route, fictional targets); a real-world WebGL basemap would need live map-tile infrastructure and would misleadingly suggest the vessel is operating in a real, identifiable location. Instead, `NavigationCanvas` is a bespoke Canvas2D renderer with its own requestAnimationFrame interpolation loop, historical/predicted tracks, CPA/TCPA geometry, layer toggles and view modes (North Up / Course Up / Vessel Centred) — the same functional requirements, without a basemap dependency or an implied real position.
19. **No React Three Fiber / Three.js 3D Digital Twin.** The Digital Twin is a 2D functional-system topology (`VesselTopology`) showing the eight monitored system areas and their power/monitoring relationships, with click-through condition/trend/evidence detail. A full 3D vessel model was judged to add rendering-pipeline risk and build time disproportionate to its incremental value over a clear, fast, accessible 2D topology for this POC.
20. **Apache ECharts** is used for genuine streaming engineering telemetry (`StreamingChart`) where the brief specifically asked for it; the existing lightweight Recharts-based `Sparkline` is retained for a few small in-context trend indicators. Standardising on a single charting library is a reasonable follow-up cleanup, noted as a remaining item.
21. Machinery and voyage models remain the illustrative approximations described in item 5 above — ECharts and the new multivariate (cylinder-spread) signal change how the analytics are *computed and displayed* (rolling history, slope, persistence, spread), not the fact that they are synthetic, uncalibrated models.
